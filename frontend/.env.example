# 백엔드 주소. 배포 시 Vercel 환경변수에 실제 배포된 백엔드 URL을 넣는다.
# 예) VITE_API_BASE_URL=https://rise-server-production.up.railway.app
#
# 이 값을 비우면 프론트가 mock 모드로 동작한다(백엔드 없이 화면만 데모).
VITE_API_BASE_URL=http://localhost:8080
